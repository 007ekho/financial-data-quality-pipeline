# requirements.txt
# Astronomer/MWAA workers — pinned for stability.
# Using Soda Core 3.5.5 — last 3.x version with stable Python API.

soda-core==3.5.5
soda-core-snowflake==3.5.5
requests>=2.31,<3.0